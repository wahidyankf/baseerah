# Behavior — web-ui-token

Gherkin behavioral specifications for
[web-ui-token](../../../../libs/web-ui-token/README.md), the shared design-token package.

## Structure

```
specs/libs/web-ui-token/behavior/
└── gherkin/
    └── tokens/
        └── tokens-export.feature
```

## Status

No test runner currently consumes these scenarios (`test:unit` is an `echo` placeholder for this
package — see the top-level [README.md](../README.md#status)).
