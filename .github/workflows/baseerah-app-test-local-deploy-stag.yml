name: baseerah-app-test-local-deploy-stag

# Heavy-test CRON pipeline for the baseerah-app app-group (web + be + e2e).
# On success force-pushes BOTH stag-baseerah-fe (Vercel preview) and
# stag-baseerah-be (triggers baseerah-be-build-deploy-stag.yml).

on:
  schedule:
    - cron: "0 20 * * *" # 3 AM WIB (UTC+7)
    - cron: "0 8 * * *" # 3 PM WIB (UTC+7)
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: ${{ github.workflow }}-${{ github.event_name == 'pull_request' && github.event.pull_request.number || github.ref }}
  cancel-in-progress: ${{ github.event_name == 'pull_request' }}

jobs:
  test-local-deploy-stag:
    uses: ./.github/workflows/_reusable-app-test-local-deploy-stag.yml
    with:
      web-project: baseerah-fe
      be-project: baseerah-be
      contracts-project: baseerah-contracts
      compose-dir: infra/dev/baseerah-app
      stag-web-branch: stag-baseerah-fe
      stag-be-branch: stag-baseerah-be
      be-port: 19320
      web-port: 19310
      environment: baseerah-app-local
