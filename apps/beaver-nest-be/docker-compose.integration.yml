# No backing services (Postgres, NATS, etc.) — baseerah-be's integration test
# (HostBootTests.fs) boots the Kestrel host in-process and calls it over real HTTP,
# with no external dependencies to wire up here.
services:
  test-runner:
    build:
      context: ../..
      dockerfile: apps/baseerah-be/Dockerfile.integration
